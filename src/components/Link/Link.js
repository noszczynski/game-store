import styled from "styled-components";
import colors from "../../utils/colors";

const Link = styled.a`
  text-decoration: none;
  color: ${colors.dark};

  &:hover {
    text-decoration: none;
    color: ${colors.dark};
  }
`;

export default Link;
